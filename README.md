# react-go

A basic example of embedding a React app in a Go binary and serving it.

It has a `-dev` flag that will proxy requests to a React development server
as well, allowing you to skip building the React app every time you make a
a change.
